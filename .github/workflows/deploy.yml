name: CI/CD Pipeline (Demo)

on:
  push:
    branches:
      - main

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: CI/CD Pipeline Demo
        run: |
          echo "CI/CD pipeline triggered successfully"
          echo "Terraform deployment step skipped in demo environment"
